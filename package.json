{
  "name": "spectral-custom-rules",
  "version": "1.0.0",
  "description": "",
  "bin": {
    "spectral": "/node_modules/@stoplight/spectral/dist/cli/index.js"
  },
  "scripts": {
    "test": "jest ./test/",
    "copyToDist": "cp -r ./ruleset/*.yaml dist && cp -r ./ruleset/.spectral.yaml dist && cp -r README.md dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@stoplight/spectral": "^5.3.0"
  },
  "devDependencies": {
    "jest": "^25.2.7"
  }
}
